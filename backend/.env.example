# Example environment file
# No API keys are required for this project.
# Services used:
# - Nominatim (geocoding) — no key
# - Overpass API (places) — no key

# You can add custom settings here in the future, for example:
# APP_LOG_LEVEL=info
